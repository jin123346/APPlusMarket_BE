<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aplus.aplusmarket.mapper.auth.UserMapper">
    <select id="selectUserById" parameterType="Long" resultType="com.aplus.aplusmarket.entity.User">
        SELECT *  FROM tb_user WHERE id = #{id}
    </select>
    <select id="selectUserByUid" parameterType="String" resultType="com.aplus.aplusmarket.entity.User">
        SELECT *  FROM tb_user WHERE uid = #{uid}
    </select>

    <select id="selectUserByEmail" parameterType="String" resultType="com.aplus.aplusmarket.entity.User">
        SELECT * FROM tb_user WHERE email = #{email}
    </select>

    <select id="selectUserByHp" parameterType="String" resultType="com.aplus.aplusmarket.entity.User">
        SELECT * FROM tb_user WHERE hp = #{hp}
    </select>

    <insert id="insertUser" parameterType="com.aplus.aplusmarket.entity.User" useGeneratedKeys="true" keyProperty="id">
        insert into tb_user(uid,password,email,hp,name,nickname,birthday)
        values (#{uid},#{password},#{email},#{hp},#{name},#{nickname}, #{birthday})
    </insert>

</mapper>
